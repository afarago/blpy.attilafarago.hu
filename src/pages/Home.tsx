import AppContent from '@/AppContent';
import React from 'react';

const Home: React.FC = () => {
    return (
        <div className="App">
            <AppContent />
        </div>
    );
};

export default Home;
